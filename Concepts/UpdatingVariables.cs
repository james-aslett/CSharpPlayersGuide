int a = 0;
a += 5; //equivalent to a = a + 5 (a = 5 after this line runs)
a -= 2; //equivalent to a = a - 2 (a = 3 after this line runs)
a *= 4; //equivalent to a = a * 4 (a = 12 after this line runs)
a /= 2; //equivalent to a = a / 2 (a = 6 after this line runs)
a %= 2; //equivalent to a = a % 2 (a = 0 after this line runs)

int b = 0;
b++; //increment - equivalent to b += 1 or b = b + 1
b--; //decrement -equivalent to b -= a % 1 or b = b - 1